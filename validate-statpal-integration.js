#!/usr/bin/env node

// StatPal integration scripts are deprecated and intentionally disabled.
// Keeping a minimal script that exits cleanly so tooling and CI won't
// fail on parsing or execution.

console.log("\n⚠️  StatPal integration scripts are deprecated and disabled.");
console.log("Use SportMonks and Football-Data verification tools instead.");
process.exit(0);
