# ===================================================================
# Node dependencies and lockfile handling
# ===================================================================
# Ignore node_modules (always)
node_modules/

# Keep package-lock.json tracked for reproducible installs
# (do NOT ignore package-lock.json)
# package-lock.json

# npm debug logs
npm-debug.log*
yarn-error.log*

# ===================================================================
# Environment files
# ===================================================================
# Ignore local env files that contain secrets
.env
.env.local
.env.*.local

# Ignore variations used in local testing/exports
.env.local*

# Ignore local helper scripts that may contain keys or tokens
scripts/run-embeds-session-alt.ps1
scripts/run-embeds-session.ps1
scripts/run-test-index-session.ps1
scripts/run-worker-bg-direct.ps1
scripts/run-worker-session.ps1
scripts/test-azure-embeddings-explicit.mjs

# ===================================================================
# Cache, temp, and logs
# ===================================================================
.cache/
tmp_*.json
*.log
cleanup-run.log
.DS_Store
Thumbs.db

# ===================================================================
# Build output and distribution
# ===================================================================
dist/
build/
.next/

# ===================================================================
# IDE and editor files
# ===================================================================
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.iml
.project
.classpath

# ===================================================================
# Docker and CI artifacts
# ===================================================================
# Render uses render.yaml; keep Docker files ignored if you don't use them
Dockerfile
docker-compose.yml
.dockerignore

# ===================================================================
# Replit notes
# ===================================================================
# .replit and replit.md are required for Replit projects — do not ignore them
# (kept here as a reminder; do not add .replit or replit.md to ignore)

# ===================================================================
# Explicit allow rules for critical repo files and folders
# ===================================================================
# Ensure source and key config files are tracked
!src/
!package.json
!render.yaml
!replit.md

# ===================================================================
# Optional: lockfile policy
# ===================================================================
# If your team prefers to commit lockfiles, keep package-lock.json tracked.
# If you intentionally want to ignore lockfiles, uncomment the next line:
# package-lock.json.history/

.history/
