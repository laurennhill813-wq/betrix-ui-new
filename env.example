## RapidAPI configuration (used by `src/api` clients)
# Provide your RapidAPI key and optional host overrides here.
RAPIDAPI_KEY=
RAPIDAPI_HOST_FREE_FOOTBALL=free-football-api-data.p.rapidapi.com
RAPIDAPI_HOST_NFL=nfl-api-data.p.rapidapi.com


---

## **File 3: DEPLOYMENT.md**

**Path:** `DEPLOYMENT.md`

**Paste this:**
```markdown
# Deployment Guide

## Replit (Easiest)
1. Click Publish button
2. Get deployment URL
3. Register Telegram webhook:
```bash
curl -X POST "https://api.telegram.org/bot$TOKEN/setWebhook" \
  -d '{"url": "YOUR_REPLIT_URL/webhook"}'# Redis connection (example values for non-secrets)
# Prefer TLS (secure) connection when available
# - `REDIS_TLS_URL` : use `rediss://host:port` for secure TLS connections
# - `REDIS_URL`     : fallback `redis://host:port` if TLS is not available
# Example (local insecure): REDIS_URL=redis://localhost:6379
# Example (cloud TLS): REDIS_TLS_URL=rediss://:password@redis-0001.example.com:6379
